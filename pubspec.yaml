name: dyson_spherec_calculator
description: A  calculator for the game "Dyson Sphere Program".

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - graphics/image_empty.png
    - graphics/image_no_net.png
    - graphics/image_error.png